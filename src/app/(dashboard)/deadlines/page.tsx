import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function DeadlinesPage() {
  return (
    <PagePlaceholder
      title="Contract Deadlines"
      description="Central deadline tracker for all active transactions with plain-language status labels. This section will be built in a future milestone."
    />
  );
}
