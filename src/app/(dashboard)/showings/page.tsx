import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function ShowingsPage() {
  return (
    <PagePlaceholder
      title="Showings"
      description="Track showing appointments, property feedback, buyer notes, and follow-up items. This section will be built in a future milestone."
    />
  );
}
