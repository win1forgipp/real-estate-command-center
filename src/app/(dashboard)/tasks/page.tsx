import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Simple tasks tied to clients, properties, transactions, or general business items. This section will be built in a future milestone."
    />
  );
}
